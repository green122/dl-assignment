import React from 'react';
import PropTypes from 'prop-types';

const Question = props => {
  return (
    <div>

    </div>
  );
};

Question.propTypes = {
  
};

export default Question;
